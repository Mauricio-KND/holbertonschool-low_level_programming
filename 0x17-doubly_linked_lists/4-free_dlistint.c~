#include "lists.h"

/**
 * free_dlistint - frees a dlink list
 * @head: pointer to the head of a dlink list
 * Return: nothing
 */
void free_dlistint(dlistint_t *head)
{
	dlistint_t *ptr = head;

	for (; ptr; ptr = head)
	{
		head = head->next;
		free(ptr);
	}
}
