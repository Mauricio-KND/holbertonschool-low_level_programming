#include <stdio.h>
/**
 *main - Prints numbers base 16 in lowercase, followed by a new line.
 *Return: An integer.
 */
int main(void)
{
	int i = "0";
	char c;

	while (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	for (c = "a"; c < "g"; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
